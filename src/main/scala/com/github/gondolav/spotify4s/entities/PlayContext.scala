package com.github.gondolav.spotify4s.entities

private[spotify4s] case class PlayContextJson()

case class PlayContext()

object PlayContext {
 private[spotify4s] def fromJson(json: PlayContextJson): PlayContext = {
  ???
 }
}